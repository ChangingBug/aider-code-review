# ============================================
# Aider Code Review 环境变量配置
# 部署时请复制此文件为 .env 并填写实际值
# ============================================

# ==================== vLLM 配置 ====================
# vLLM API 地址
VLLM_API_BASE=http://192.168.1.100:8000/v1

# vLLM API 密钥（vLLM通常不需要，但Aider可能校验）
VLLM_API_KEY=sk-xxx

# 模型名称（使用 openai/ 前缀让Aider使用OpenAI协议）
VLLM_MODEL_NAME=openai/qwen-2.5-coder-32b

# ==================== Git 平台配置 ====================
# Git平台类型: gitlab, gitea, github
GIT_PLATFORM=gitlab

# Git API 地址
GIT_API_URL=http://gitlab.internal/api/v4

# Git 访问令牌
# GitLab: Private Token
# Gitea/GitHub: Personal Access Token
GIT_TOKEN=your-private-token-here

# ==================== Aider 配置 ====================
# RepoMap Token数量（较大值提供更好的上下文，但消耗更多显存）
# 默认值适配32B模型的262144上下文窗口
AIDER_MAP_TOKENS=262144

# 是否禁用RepoMap（显存不足时设为true）
AIDER_NO_REPO_MAP=false

# ==================== 服务配置 ====================
# 服务端口
SERVER_PORT=5000

# 日志级别: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# SSH密钥路径（用于克隆私有仓库）
SSH_KEY_PATH=~/.ssh
